[server]
maxUploadSize = 0
# Disable XSRF so CORS can be false behind reverse proxy (CapRover); dashboard is not public edit
enableXsrfProtection = false
enableCORS = false
headless = true
address = "0.0.0.0"
port = 8501

[browser]
gatherUsageStats = false

[theme]
base = "dark"
